---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: switches.unifabric.io
spec:
  group: unifabric.io
  names:
    categories:
    - switch
    kind: Switch
    listKind: SwitchList
    plural: switches
    shortNames:
    - sw
    singular: switch
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mgmtIP
      name: MgmtIP
      type: string
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .status.healthy
      name: Healthy
      type: boolean
    - jsonPath: .status.lldpNeighborCount
      name: Neighbors
      type: integer
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              grpcPort:
                description: GrpcPort overrides the default switch-agent gRPC listen
                  port for this switch.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              mgmtIP:
                description: MgmtIP is the controller dial target for this switch.
                minLength: 1
                type: string
              role:
                default: ScaleOut
                description: Role classifies this switch by fabric domain.
                enum:
                - ScaleOut
                - ScaleUp
                - Storage
                type: string
            required:
            - mgmtIP
            type: object
          status:
            properties:
              conditions:
                description: Conditions describe the current switch stream and data
                  freshness state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              healthy:
                type: boolean
              hostname:
                description: Hostname is the switch-reported system name from the
                  latest accepted snapshot.
                type: string
              lldpNeighborCount:
                description: LLDPNeighborCount reports the number of stored LLDP neighbor
                  entries.
                format: int32
                type: integer
              lldpNeighbors:
                description: LLDPNeighbors stores unique remote neighbors observed
                  for this switch.
                items:
                  properties:
                    remoteSystemName:
                      description: RemoteSystemName identifies the remote system for
                        this LLDP entry.
                      type: string
                    remoteSystemType:
                      description: RemoteSystemType identifies whether the remote
                        system resolves to a Kubernetes Node or another switch.
                      enum:
                      - KubernetesNode
                      - Switch
                      type: string
                  required:
                  - remoteSystemName
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
